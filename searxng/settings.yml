use_default_settings: true

general:
  instance_name: "localhost.ai"

search:
  safe_search: 0
  autocomplete: ""
  default_lang: ""
  formats:
    - html
    - json

server:
  bind_address: "0.0.0.0"
  port: 8080
  secret_key: "change-me-localhost-ai"
  limiter: false
  image_proxy: false

ui:
  static_use_hash: true
